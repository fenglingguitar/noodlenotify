<?xml version="1.0" encoding="UTF-8"?>
<dynamic-hibernate>
	<query name="customer-group-query-list">
	<![CDATA[
		SELECT 
				customerGroup_Nm,
				manual_Status
		FROM 	CSL_CUSTOMER_GROUP
		WHERE 	1=1
		 		#if($customerGroup_Nm)
					AND customerGroup_Nm LIKE :customerGroup_Nm
				#end
		 		#if($manual_Status)
					AND manual_Status = :manual_Status
				#end
		ORDER BY customerGroup_Nm
	]]>
	</query>
	<query name="customergroup-query-includelist">
	<![CDATA[
		SELECT 
				c.customer_Id,
				c.name,
				c.ip,
				c.port,
				c.url,
				c.type,
				c.check_Port,
				c.check_Url,
				c.check_Type,
				c.system_Status,
				c.manual_Status,
				c.customerGroup_Nm
		FROM 	CSL_CUSTOMER c
		WHERE 	1=1
				#if($name)
					AND c.name LIKE :name
				#end
		 		#if($ip)
					AND c.ip LIKE :ip
				#end
		 		#if($port)
					AND c.port = :port
				#end
				#if($type)
					AND c.type = :type
				#end
		 		#if($check_Port)
					AND c.check_Port = :check_Port
				#end
				#if($check_Type)
					AND c.check_Type = :check_Type
				#end
		 		#if($system_Status)
					AND c.system_Status = :system_Status
				#end
		 		#if($manual_Status)
					AND c.manual_Status = :manual_Status
				#end
		 		#if($customerGroup_Nm)
					AND c.customerGroup_Nm = :customerGroup_Nm
				#end
		ORDER BY customer_Id desc
	]]>
	</query>
	
	<query name="customergroup-query-excludelist">
	<![CDATA[
		SELECT 
				c.customer_Id,
				c.name,
				c.ip,
				c.port,
				c.url,
				c.type,
				c.check_Port,
				c.check_Url,
				c.check_Type,
				c.system_Status,
				c.manual_Status,
				c.customerGroup_Nm
		FROM 	CSL_CUSTOMER c
		WHERE 	1=1
				#if($name)
					AND c.name LIKE :name
				#end
		 		#if($ip)
					AND c.ip LIKE :ip
				#end
		 		#if($port)
					AND c.port = :port
				#end
				#if($type)
					AND c.type = :type
				#end
		 		#if($check_Port)
					AND c.check_Port = :check_Port
				#end
				#if($check_Type)
					AND c.check_Type = :check_Type
				#end
		 		#if($system_Status)
					AND c.system_Status = :system_Status
				#end
		 		#if($manual_Status)
					AND c.manual_Status = :manual_Status
				#end
		 		AND c.customerGroup_Nm is null
		 ORDER BY customer_Id DESC
	]]>
	</query>

</dynamic-hibernate>